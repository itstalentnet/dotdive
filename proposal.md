# dotdive — پروپوزال و بلوپرینت پیاده‌سازی

> نسخهٔ ۱.۱ — پیش‌نویس برای توسعه‌دهنده
> «دانشنامه و نقطهٔ شیرجه زدن به پروژه‌ها»
> **تغییرات نسخهٔ ۱.۱:** بیلد دومسیره (استاتیک + رانتایم) در بخش ۵.۱، فقط Markdown، و مشخصات موبایل‌فرست در بخش ۱۰.۶

## ۰. نحوهٔ خواندن این سند

- **تصمیم:** مواردی که قطعی هستند و بدون هماهنگی عوض نمی‌شوند.
- **پیش‌فرض:** پیشنهاد من است. توسعه‌دهنده می‌تواند با ذکر دلیل عوضش کند.
- بخش ۱۹ فهرست سؤال‌های باز است که هر کدام پیش‌فرضی دارد؛ لازم نیست کار به‌خاطر آن‌ها متوقف شود.
- اصطلاحات فنی به همان شکل انگلیسی نوشته شده‌اند.

---

## ۱. خلاصهٔ پروژه

**dotdive** یک وب‌سایت دانشنامه و مستندات کاملاً فارسی است، با ظاهر مدرن، مینیمال و شیک در کیفیتی نزدیک به Notion.

- محتوا فقط **فایل‌های Markdown در گیت‌هاب** است (پوشهٔ `docs/` در مونوریپو). هیچ دیتابیسی برای محتوا وجود ندارد.
- اپلیکیشن در پوشهٔ `web/` همان مونوریپو قرار دارد.
- ریشهٔ `public` شامل **لندینگ، وبلاگ و مستندات عمومی** است؛ برای همه باز است و کاملاً SEO‌فرندلی.
- بقیهٔ ریشه‌ها (`nons`، `lemmo` و هر ریشهٔ آینده) **پشت احراز هویت** هستند.
- ورود با **گوگل** یا **کد یک‌بارمصرف ایمیل** انجام می‌شود. فهرست ایمیل‌های مجاز در `.env` است. ثبت‌نام وجود ندارد.
- **بیلد دومسیره (استاتیک + رانتایم):** در زمان بیلد، ریشهٔ `public` به سایت استاتیک تبدیل می‌شود و ریشه‌های خصوصی جداگانه رندر می‌شوند و در مسیری مخصوص سرور می‌مانند که فقط پشت احراز هویت سرو می‌شود (بخش ۵.۱). بدون ISR و بدون پیچیدگی اضافه.
- **مستندات همیشه Markdown ساده** است و فقط به وب رندر می‌شود (بدون MDX).
- **موبایل‌فرست:** همهٔ صفحات روی موبایل درجه‌یک هستند (بخش ۱۰.۶).
- جستجوی قوی، آگاه از سطح دسترسی و مخصوص فارسی.
- برای بخش عمومی: `sitemap.xml`، `llms.txt`، RSS و نسخهٔ خام Markdown هر صفحه.
- پروژه **مستقل** توسعه داده می‌شود. سرور MCP بعداً در همین مونوریپو اضافه می‌شود (یا دربارهٔ آن تصمیم آینده گرفته می‌شود)، پس معماری از الان باید آمادهٔ آن باشد (بخش ۱۸).

---

## ۲. اهداف و غیراهداف

### اهداف

1. خواندن و مرور راحت و زیبای مستندات فارسی (RTL درجه‌یک)
2. جداسازی امن محتوای عمومی و خصوصی، بدون هیچ نشتی
3. جستجوی سریع و دقیق برای فارسی، فقط در محدودهٔ دسترسی کاربر
4. اضافه کردن محتوا فقط با commit کردن فایل Markdown
5. سئو و قابلیت خوانده شدن توسط مدل‌های هوش مصنوعی برای بخش عمومی
6. سادگی نگهداری: کم‌ترین قطعهٔ متحرک ممکن

### غیراهداف (نسخهٔ ۱)

- ثبت‌نام، پروفایل کاربر، نقش‌های پیچیده
- پنل مدیریت و ویرایشگر آنلاین
- دیتابیس محتوا
- کامنت و تعامل اجتماعی
- چندزبانه (فقط فارسی)
- سرور MCP و API نوشتن (فاز بعد)

---

## ۳. اصطلاحات

| اصطلاح | معنی |
|---|---|
| **ریشه (Root)** | هر پوشهٔ سطح اول داخل `docs/` (مثل `nons`، `lemmo`، `public`). هر ریشه یک پروژه یا فضای محتوایی است. |
| **گره (Node)** | هر پوشه، هر فایل، و هر بخش (heading) داخل فایل |
| **فایل ایندکس** | فایل `index.md` هر پوشه که نام، توضیح، آیکون و ترتیب آن پوشه را تعریف می‌کند |
| **بخش (Section)** | heading‌های `##` و `###` یک فایل؛ فرزندان همان فایل به حساب می‌آیند |
| **Principal / AccessContext** | هویت درخواست‌دهنده و ریشه‌هایی که می‌تواند بخواند |

---

## ۴. مدل محتوا

### ۴.۱ ساختار پوشه‌ها

```
repo/
├─ docs/
│  ├─ public/                     # ریشهٔ عمومی
│  │  ├─ index.md                 # توضیح ریشه + متن‌های لندینگ
│  │  ├─ blog/
│  │  │  ├─ index.md
│  │  │  └─ hello-dotdive.md
│  │  └─ docs/
│  │     ├─ index.md
│  │     └─ getting-started/
│  │        ├─ index.md
│  │        ├─ 01-intro.md
│  │        └─ 02-concepts.md
│  ├─ nons/                       # ریشهٔ خصوصی
│  │  ├─ index.md
│  │  ├─ backend/
│  │  │  ├─ index.md
│  │  │  └─ auth.md
│  │  └─ frontend/
│  │     ├─ index.md
│  │     └─ img/diagram.png       # پیوست‌ها کنار فایل‌ها
│  └─ lemmo/
│     └─ ...
├─ web/                           # اپلیکیشن
└─ ...
```

### ۴.۲ قواعد

- **هر پوشه یک `index.md` دارد.** فایل ایندکس عنوان، توضیح، آیکون و ترتیب پوشه را می‌دهد و در منو، breadcrumb، صفحهٔ پروژه‌ها و جستجو استفاده می‌شود. اگر نباشد، اسم پوشه به‌عنوان عنوان می‌آید و در `content:check` هشدار می‌دهد.
- **هر فایل یک صفحهٔ مستقل است** و heading‌های `##` و `###` آن فرزندانش هستند. آن‌ها در TOC، منوی جانبی (زیر صفحهٔ فعال) و جستجو دیده می‌شوند و با `#anchor` قابل لینک‌اند.
- **دسترسی در سطح ریشه تعریف می‌شود، نه فایل.** تصمیم: ریشهٔ `public` برای همه باز است و بقیه خصوصی‌اند. لازم نیست هر فایل `visibility` جدا داشته باشد.
- **ترتیب نمایش:** اول `order` در front matter، بعد پیشوند عددی نام فایل (`01-`)، بعد ترتیب الفبایی فارسی. پیشوند عددی از URL حذف می‌شود.
- **slug:** پیش‌فرض نام فایل (بدون پیشوند عددی و پسوند). با فیلد `slug` قابل تغییر است. توصیه: نام فایل‌ها انگلیسی باشد.
- **پیوست‌ها** (`png، jpg، webp، svg، gif، pdf، mp4`) کنار فایل‌ها می‌مانند تا دسترسی‌شان از پوشه ارث ببرد.
- **انواع فایل:** `.md` در نسخهٔ ۱ پشتیبانی می‌شود. فایل‌های دیگر (مثلاً `.json` یا `.txt`) در نسخهٔ ۱ نادیده گرفته می‌شوند و بیلد یک هشدار می‌دهد، مگر پیوست باشند. MDX و کامپوننت داخل محتوا وجود ندارد؛ مستندات همیشه Markdown ساده (GFM) است.
- **پیش‌نویس:** `draft: true` در بخش عمومی اصلاً بیلد نمی‌شود. در ریشه‌های خصوصی با نشان «پیش‌نویس» نمایش داده می‌شود.

### ۴.۳ Front matter

| فیلد | نوع | توضیح |
|---|---|---|
| `title` | string | اگر نباشد، از اولین `# heading` گرفته می‌شود |
| `description` | string | توضیح کوتاه (برای meta، کارت‌ها، جستجو) |
| `icon` | emoji | آیکون کنار عنوان، مثل Notion |
| `cover` | path | تصویر کاور (وبلاگ و لندینگ) |
| `order` | number | ترتیب در منو |
| `tags` | string[] | تگ‌ها |
| `slug` | string | جایگزین نام فایل |
| `aliases` | string[] | slugهای قدیمی برای redirect دائمی (301) |
| `draft` | boolean | پیش‌نویس |
| `hidden` | boolean | از منو و جستجو پنهان، اما با لینک مستقیم باز می‌شود |
| `updated` | date | جایگزین تاریخ git (بخش ۸) |
| `noindex` | boolean | فقط برای صفحات عمومی؛ حذف از sitemap و meta robots |
| `date`، `author` | | فقط برای پست‌های وبلاگ |
| `collapsed` | boolean | فقط برای `index.md`؛ پوشه در منو بسته شروع شود |

نمونه:

```yaml
---
title: احراز هویت
description: جریان ورود و مدیریت نشست‌ها در بک‌اند
icon: 🔐
tags: [auth, backend]
order: 2
---
```

اعتبارسنجی با `zod` در زمان بیلد انجام می‌شود. front matter نامعتبر یعنی **بیلد شکست می‌خورد** و پیام خطا مسیر فایل را می‌گوید.

### ۴.۴ نگاشت `public` به وب

| مسیر در ریپو | آدرس در سایت |
|---|---|
| `docs/public/index.md` | `/` (متن‌های لندینگ از front matter و بدنه) |
| `docs/public/blog/*.md` | `/blog/[slug]` |
| `docs/public/docs/**` | `/docs/...` |
| `docs/<root>/**` (غیر از public) | `/p/<root>/...` |

---

## ۵. معماری

```
GitHub monorepo (PRIVATE)
 ├─ docs/  ← محتوا
 └─ web/   ← اپلیکیشن
        │  push → build
        ▼
  ┌──────────────────────────────────────────────┐
  │  Build-time: content pipeline                │
  │   scan docs/ → validate → manifest           │
  │   render markdown → HTML/AST                 │
  │   build search docs per root                 │
  │   output → out/public + out/private          │
  └───────────────────┬──────────────────────────┘
                      ▼
  ┌──────────────────────────────────────────────┐
  │  Runtime: Next.js server                     │
  │                                              │
  │  Public routes  → static (SSG) + CDN         │
  │  Private routes → dynamic, per-request auth  │
  │                                              │
  │  Access layer ── همهٔ خواندن‌ها از اینجا رد  │
  │  Search API   ── ایندکس per-root، فیلتر مجوز │
  │  Auth         ── Google + Email OTP + JWT    │
  └───────────────────┬──────────────────────────┘
                      ▼
         Ephemeral store (فقط کدهای OTP و rate limit)
```

### ۵.۱ بیلد دومسیره: استاتیک + رانتایم

اصل: **یک بیلد، دو خروجی.** یک اسکریپت واحد (`build-content`) همهٔ `docs/` را می‌خواند، Markdown را فقط یک بار به HTML رندر می‌کند و بر اساس ریشه، خروجی را در دو جای جدا می‌گذارد:

```
docs/public/**   ──►  out/public/          استاتیک: HTML، فایل‌های خام .md، sitemap.xml،
                                            llms.txt، rss.xml، تصاویر OG، ایندکس جستجوی عمومی
                                            ← روی هر هاست استاتیک یا CDN قابل سرو است

docs/<root>/**   ──►  out/private/<root>/  رندرشده: HTML + متادیتا + heading‌ها +
                                            ایندکس جستجوی همان ریشه + پیوست‌ها
                                            ← فقط سمت سرور؛ هرگز در مسیر استاتیک یا CDN
```

| | مسیر استاتیک (`public`) | مسیر رانتایم (ریشه‌های خصوصی) |
|---|---|---|
| زمان رندر Markdown | بیلد | بیلد (در رانتایم Markdown پردازش نمی‌شود) |
| سرو | فایل مستقیم، بدون کد سرور | سرور کوچک: بررسی نشست ← خواندن از `out/private/` ← پاسخ |
| احراز هویت | ندارد | دارد |
| کش | CDN، طولانی | `no-store` |
| جستجو | ایندکس JSON استاتیک، سمت کلاینت | `/api/search` سمت سرور |
| نمونه آدرس | `/docs/getting-started/intro` | `/p/nons/backend/auth` |

قواعد سادگی:

- **بدون ISR، بدون revalidate، بدون دیتابیس محتوا.** انتشار یعنی build + deploy.
- **بدون MDX و بدون کامپوننت داخل محتوا:** فقط Markdown ساده (GFM) به‌علاوهٔ callout و Mermaid.
- **چیدمان مشترک:** منو، TOC، هدر و تایپوگرافی یک مجموعه کامپوننت برای هر دو مسیر است تا ظاهر یکسان بماند.
- **همهٔ فایل‌های عمومی در بیلد ساخته می‌شوند:** sitemap، robots، `llms.txt`، `llms-full.txt`، RSS، نسخه‌های `.md` و تصاویر OG؛ رانتایم فقط ورود، صفحات خصوصی، جستجوی خصوصی و پیوست‌های خصوصی را سرو می‌کند.
- **در Next.js:** صفحات عمومی با `generateStaticParams` و `dynamicParams = false` (هیچ صفحهٔ عمومی پویا وجود ندارد)؛ صفحات خصوصی `force-dynamic` که فقط از `out/private/` می‌خوانند. برای Docker از `output: 'standalone'` استفاده شود.
- **جایگزین سبک‌تر:** اگر توسعه‌دهنده Next.js را برای این کار سنگین می‌داند، یک اسکریپت بیلد + سرور Hono (فایل‌های استاتیک و مسیرهای خصوصی) همین معماری را بدون تغییر پیاده می‌کند.
- **ارتقای آینده:** چون `out/public/` مستقل است، بعداً می‌شود آن را روی CDN گذاشت و فقط رانتایم را جدا نگه داشت؛ در نسخهٔ ۱ لازم نیست و دو دامنه/کوکی/CORS پیچیدگی می‌آورد.

### پشتهٔ پیشنهادی (پیش‌فرض)

| بخش | انتخاب |
|---|---|
| فریمورک | **Next.js (App Router) + TypeScript** |
| استایل | Tailwind CSS + primitiveهای Radix (shadcn/ui به‌عنوان پایه، با تم سفارشی) |
| Markdown | `unified` (remark/rehype)، `remark-gfm`، `rehype-slug`، `rehype-autolink-headings`، `shiki` برای هایلایت، `rehype-sanitize` |
| جستجو | MiniSearch (یا Orama) با tokenizer و پردازش فارسی سفارشی |
| احراز هویت | یک کتابخانهٔ استاندارد (پیشنهاد: Better Auth، یا Auth.js) با Google + email OTP |
| ایمیل | Resend یا هر SMTP دیگر |
| ذخیرهٔ موقت | Upstash Redis (پلن رایگان) یا SQLite روی VPS؛ فقط برای OTP و rate limit |
| تست | Vitest (واحد) + Playwright (e2e) |
| پکیج‌منیجر | pnpm |
| CI | GitHub Actions |

> برای انتخاب کتابخانهٔ احراز هویت و پلن‌های رایگان (ایمیل، Redis، میزبانی)، توسعه‌دهنده باید در شروع پروژه مستندات و محدودیت‌های فعلی را بررسی کند؛ این‌ها زود تغییر می‌کنند.

چرا Next.js: هم صفحات استاتیک برای سئو، هم صفحات پویا و مجوزدار، هم route handler برای `llms.txt`، sitemap، جستجو و OG image در یک پروژه. Astro هم گزینهٔ معتبری است، ولی بخش خصوصی و احراز هویت کار سفارشی بیشتری دارد؛ عوض کردنش نیاز به دلیل مشخص دارد.

### میزبانی (پیش‌فرض)

- **ساده‌ترین (پیش‌فرض):** یک سرور واحد (Docker روی VPS، یا Vercel) که هم فایل‌های استاتیک عمومی و هم رانتایم خصوصی را سرو می‌کند. شرایط پلن رایگان Vercel (استفادهٔ غیرتجاری) را قبل از انتخاب بررسی کن.
- **گزینهٔ جداشده (اختیاری، بعداً):** بخش عمومی روی CDN/هاست استاتیک و فقط بخش خصوصی روی رانتایم.
- هر commit روی `main` یک deploy جدید می‌سازد؛ انتشار محتوا یعنی همین.
- ریپو حتماً **Private** است.

---

## ۶. مسیرها (Routes)

| مسیر | دسترسی | توضیح |
|---|---|---|
| `/` | عمومی | لندینگ |
| `/blog` | عمومی | فهرست پست‌ها، صفحه‌بندی |
| `/blog/[slug]` | عمومی | پست |
| `/blog/tag/[tag]` | عمومی | فیلتر تگ |
| `/docs`، `/docs/[...path]` | عمومی | مستندات عمومی |
| `/login` | عمومی | صفحهٔ ورود |
| `/projects` | ورود لازم | «نقطهٔ شیرجه»: کارت پروژه‌های قابل‌دسترس کاربر |
| `/p/[root]/[...path]` | ورود + دسترسی به ریشه | مستندات خصوصی |
| `/api/auth/*` | | ورود، خروج، callback |
| `/api/search` | رانتایم | نتایج ریشه‌های خصوصی طبق دسترسی کاربر (جستجوی عمومی سمت کلاینت است) |
| `/api/asset/[root]/...` | ورود + دسترسی | پیوست‌های خصوصی |
| `/sitemap.xml` | عمومی | فقط بخش عمومی |
| `/robots.txt` | عمومی | |
| `/rss.xml` | عمومی | فید وبلاگ |
| `/llms.txt`، `/llms-full.txt` | عمومی | فقط بخش عمومی |
| `/docs/x.md`، `/blog/x.md` | عمومی | نسخهٔ خام Markdown هر صفحهٔ عمومی |
| `/og/...` | عمومی | تصویر Open Graph صفحات عمومی |

نام‌های رزرو (نباید ریشه یا slug شوند): `blog، docs، p، projects، login، api، og`.

---

## ۷. احراز هویت و دسترسی

### ۷.۱ قواعد دسترسی

- **ریشهٔ `public`:** برای همه.
- **هر ریشهٔ دیگر:** فقط ایمیل‌هایی که در `.env` برای آن ریشه (یا برای «همه») ثبت شده‌اند.
- **پیش‌فرض ردکردن:** ریشه‌ای که هیچ ایمیلی برایش تعریف نشده، برای هیچ‌کس (جز `ACCESS_ALL`) باز نیست. ریشهٔ جدید بدون تغییر `.env` خصوصی می‌ماند.
- سطح دسترسی در نسخهٔ ۱ فقط **خواندن** است.
- مقایسهٔ ایمیل: `trim` و `lowercase`. بدون حذف نقطه یا `+` در جیمیل.

### ۷.۲ قالب `.env`

```bash
# همهٔ ریشه‌های خصوصی
ACCESS_ALL=owner@example.com

# دسترسی به یک ریشه (نام پوشه به حروف بزرگ، خط تیره → زیرخط)
ACCESS_NONS=a@example.com,b@example.com
ACCESS_LEMMO=c@example.com
```

کد باید ریشه‌ها را از روی پوشه‌های `docs/` کشف کند و متغیر `ACCESS_<ROOT>` را برایشان بخواند؛ اضافه کردن ریشهٔ جدید نیاز به تغییر کد ندارد.

### ۷.۳ روش‌های ورود

**گوگل**
- OAuth 2.0 / OIDC، با `state` و `nonce`
- فقط وقتی `email_verified = true` باشد
- نشست فقط برای ایمیل‌هایی ساخته می‌شود که در فهرست مجاز باشند؛ در غیر این‌صورت پیام «این ایمیل دسترسی ندارد»

**کد ایمیل (Magic code)**
1. کاربر ایمیل را وارد می‌کند.
2. سرور همیشه پاسخ یکسان می‌دهد: «اگر این ایمیل مجاز باشد، کدی ارسال شده است.» (جلوگیری از enumeration)
3. فقط اگر ایمیل مجاز باشد، یک کد ۶ رقمی (با `crypto` امن) ارسال می‌شود.
4. کد **هش‌شده** (HMAC با secret + ایمیل) ذخیره می‌شود، نه متن ساده.
5. اعتبار ۱۰ دقیقه، **یک‌بارمصرف**، حداکثر ۵ تلاش برای هر کد.
6. ارسال مجدد بعد از ۶۰ ثانیه؛ سقف ۵ درخواست در ساعت برای هر ایمیل و ۲۰ در ساعت برای هر IP.
7. قالب ایمیل فارسی و RTL، همراه نسخهٔ متنی ساده. دامنهٔ فرستنده باید SPF، DKIM و DMARC داشته باشد.

### ۷.۴ نشست

- کوکی `HttpOnly`، `Secure`، `SameSite=Lax`، اعتبار ۳۰ روز.
- داخل نشست فقط ایمیل و زمان صدور است؛ **ریشه‌های مجاز در هر درخواست از `.env` محاسبه می‌شوند.** با حذف ایمیل از `.env` و deploy مجدد، دسترسی بلافاصله قطع می‌شود.
- پارامتر `next` فقط مسیر نسبی پذیرفته می‌شود (با `/` شروع شود و با `//` نه).
- محافظت CSRF با بررسی `Origin` روی درخواست‌های `POST`.

### ۷.۵ رفتار یکسان (ضد نشتی)

| موقعیت | رفتار |
|---|---|
| ناشناس، هر مسیر زیر `/p/**` (حتی نامعتبر) | ریدایرکت به `/login?next=...` |
| واردشده، بدون دسترسی به ریشه، یا صفحه وجود ندارد | **همان صفحهٔ ۴۰۴** |
| ناشناس، `/api/search` | فقط نتایج عمومی |
| هدرهای همهٔ مسیرهای خصوصی | `Cache-Control: private, no-store` و `X-Robots-Tag: noindex, nofollow, noarchive` |

### ۷.۶ الزام «دفاع در عمق»

مجوز فقط در middleware چک نشود. **لایهٔ دسترسی به داده (بخش ۸) خودش هر خواندن را با `AccessContext` بررسی می‌کند.** آسیب‌پذیری‌های دورزدن middleware در فریمورک‌های مشابه قبلاً گزارش شده است، پس هیچ مسیری نباید فقط به middleware تکیه کند.

---

## ۸. لایهٔ محتوا (Content Core)

این لایه قلب پروژه است و باید **مستقل از UI** نوشته شود (پوشهٔ جدا با مرز مشخص؛ اگر لازم شد بعداً به package جدا استخراج می‌شود). MCP آینده مستقیم از همین لایه استفاده می‌کند.

### در زمان بیلد

1. اسکن `docs/`، اعتبارسنجی front matter، ساخت **manifest** (درخت گره‌ها، متادیتا، heading‌ها)
2. رندر Markdown به HTML/AST (با sanitize)
3. تولید رکوردهای جستجو برای هر ریشه (یک رکورد به‌ازای هر بخش)
4. خروجی به دو مسیر جدا می‌رود (بخش ۵.۱): `out/public/` (استاتیک) و `out/private/<root>/` (فقط سرور). محتوای خصوصی هرگز در `out/public/`، `web/public/` یا bundle کلاینت قرار نمی‌گیرد.
5. اعتبارسنجی لینک‌ها: لینک شکسته یا anchor ناموجود = خطا
6. **لینک از صفحهٔ عمومی به ریشهٔ خصوصی = خطای بیلد**

### API داخلی (توابع خالص، همه با `AccessContext`)

```ts
type AccessContext = { email: string | null; roots: Set<string> | 'all' };

listRoots(ctx): Root[]
getTree(rootId, ctx): TreeNode[]
getPage(path, ctx): Page | null        // null = وجود ندارد یا دسترسی ندارد (یکسان)
search(query, ctx, opts): SearchHit[]
getPublicSitemap(): SitemapEntry[]     // بدون ctx؛ فقط عمومی
getPublicLlmsIndex(): LlmsEntry[]
```

### تاریخ آخرین ویرایش

از تاریخچهٔ git خوانده می‌شود، اما clone کم‌عمق (shallow) در بعضی پلتفرم‌های deploy تاریخچهٔ کامل ندارد. اولویت: `updated` در front matter، بعد git، بعد تاریخ بیلد. توسعه‌دهنده باید رفتار پلتفرم انتخابی را بررسی کند.

### در حالت توسعه

watch روی `docs/` و به‌روزرسانی بدون restart.

### دستور `content:check`

اعتبارسنجی front matter، `index.md` مفقود، slug تکراری، لینک شکسته، لینک عمومی→خصوصی، پیوست بدون استفاده. در CI اجرا می‌شود.

---

## ۹. جستجو

- **ایندکس جدا برای هر ریشه**، در حافظه و lazy load. کوئری فقط روی ریشه‌هایی اجرا می‌شود که کاربر به آن‌ها دسترسی دارد (+ `public`)؛ فیلتر **قبل از** ساخت نتیجه و snippet.
- واحد ایندکس: **هر بخش** (heading) یک رکورد؛ نتیجه به `#anchor` همان بخش لینک می‌شود و در UI بر اساس صفحه گروه‌بندی می‌شود.
- وزن فیلدها (پیش‌فرض): عنوان صفحه ۵، heading ۳، تگ‌ها ۳، توضیح ۲، متن ۱.
- prefix search فعال، fuzzy سبک (حدود ۰٫۲).
- نمایش snippet حدود ۱۶۰ نویسه با هایلایت.
- **جستجوی عمومی سمت کلاینت است:** یک ایندکس JSON استاتیک (`out/public/`) با همان پردازش فارسی، فقط هنگام اولین باز شدن پالت جستجو بارگذاری می‌شود. جستجوی ریشه‌های خصوصی فقط از `/api/search` سمت سرور انجام می‌شود؛ ایندکس خصوصی هرگز به کلاینت فرستاده نمی‌شود.
- API: `GET /api/search?q=...&root=...&limit=...` (با `no-store` برای هر پاسخ حاوی نتایج خصوصی).

### پردازش متن فارسی (الزامی)

1. `NFKC`
2. `ي → ی`، `ك → ک`، `ة → ه`، `أ/إ/ٱ → ا`
3. حذف کشیده (ـ) و اعراب (`U+064B–U+065F`، `U+0670`)
4. ارقام فارسی و عربی → لاتین (`۱۲۳` و `١٢٣` → `123`)
5. نیم‌فاصله (`U+200C`): هم شکل جداشده و هم شکل چسبیده ایندکس شود (`می‌شود` → `می`، `شود`، `میشود`)
6. علائم فارسی (`، ؛ ؟`) جداکننده
7. lowercase برای لاتین؛ شکستن `camelCase` و `snake_case`، با نگه داشتن توکن اصلی (برای جستجوی نام کد)
8. ریشه‌یابی (stemming) در نسخهٔ ۱ استفاده نشود؛ prefix و fuzzy کافی است

### رابط جستجو

- Command palette با `Ctrl/Cmd + K` (و `/`)
- نتایج بر اساس ریشه/صفحه گروه‌بندی می‌شوند؛ ناوبری با کیبورد؛ حالت «بدون نتیجه» و «در حال جستجو»
- اگر حجم کل محتوا از حدود چند ده مگابایت گذشت، مهاجرت به SQLite FTS5 روی VPS بررسی شود.

---

## ۱۰. رابط کاربری و تجربهٔ کاربری

### ۱۰.۱ اصول

- **فارسی و RTL از پایه**، نه ترجمهٔ یک قالب LTR. `lang="fa" dir="rtl"`. بلوک‌های کد و عبارت‌های لاتین همیشه LTR و درست ایزوله (`<bdi>`، `dir="ltr"`).
- مینیمال: فضای سفید زیاد، یک رنگ تأکید، بدون تزئین اضافی.
- کیفیت Notion: تایپوگرافی عالی، آیکون و کاور صفحه، callout، toggle، جدول تمیز، انیمیشن‌های ظریف.
- سریع: انتقال‌ها زیر ۲۰۰ms و بدون پرش چیدمان.
- فونت‌ها **self-host** شوند و هیچ وابستگی runtime به CDN یا Google Fonts نباشد.
- ارقام در رابط فارسی (`۱۲۳`)، ولی داخل کد و URL لاتین.
- تاریخ‌ها در رابط شمسی (`Intl.DateTimeFormat('fa-IR-u-ca-persian')`).

### ۱۰.۲ توکن‌های طراحی (پیشنهاد)

| توکن | روشن | تاریک |
|---|---|---|
| پس‌زمینه | `#FFFFFF` | `#0E0F11` |
| سطح ثانویه | `#F7F7F5` | `#17181B` |
| متن | `#1F2328` | `#E6E6E6` |
| متن کم‌رنگ | `#6B7280` | `#9AA0A6` |
| مرز | `#E7E7E4` | `#26282C` |
| تأکید (پیشنهاد: آبی-سبز اقیانوسی) | `#0E7490` | `#22B8CF` |

- **فونت متن:** Vazirmatn یا Estedad (variable، فقط وزن‌های لازم)، **فونت کد:** یک monospace با پشتیبانی خوب از لاتین.
- اندازهٔ متن ۱۶–۱۷px، `line-height` حدود ۱٫۹ برای متن فارسی، عرض ستون محتوا حدود ۷۲۰px.
- شعاع گوشه ۸px، سایه‌ها بسیار ملایم، حالت روشن/تاریک/سیستم با ذخیرهٔ انتخاب.
- لوگو: نقطه + موج (ایدهٔ اولیه؛ طراح نهایی می‌کند).

### ۱۰.۳ چیدمان صفحهٔ مستندات

```
┌──────────────────────────────────────────────────────────────┐
│  dotdive ▾ (سوییچ ریشه)     [ جستجو ⌘K ]      🌙   👤       │
├───────────────┬──────────────────────────────┬───────────────┤
│  منوی جانبی   │  breadcrumb                  │  در این صفحه  │
│  (سمت راست)   │  🔐 عنوان صفحه               │  (TOC، چپ)    │
│               │  توضیح · آخرین ویرایش · تگ   │  ● بخش ۱      │
│  ▸ backend    │                              │    بخش ۲      │
│    auth ●     │  متن محتوا ...               │    بخش ۳      │
│      بخش ۱    │                              │               │
│      بخش ۲    │  [قبلی]            [بعدی]    │               │
│  ▸ frontend   │                              │               │
└───────────────┴──────────────────────────────┴───────────────┘
```

- منو از `index.md` پوشه‌ها ساخته می‌شود: عنوان، آیکون، ترتیب، `collapsed`. زیر صفحهٔ فعال، heading‌های آن نشان داده می‌شود.
- موبایل: منو به‌صورت drawer، TOC جمع‌شونده در بالای صفحه.
- سوییچ ریشه فقط ریشه‌های قابل‌دسترس کاربر را نشان می‌دهد.

### ۱۰.۴ اجزای محتوا

عنوان‌ها با لینک anchor، کد با دکمهٔ کپی و نام فایل، callout (`> [!NOTE]`، `[!WARNING]`، `[!TIP]`)، جدول (اسکرول افقی روی موبایل)، فهرست وظایف، تصویر با بزرگ‌نمایی، `<details>` به‌عنوان toggle، Mermaid (بارگذاری تنبل)، پاورقی، پیوند داخلی امن، badge پیش‌نویس.

### ۱۰.۵ صفحه‌ها

- **لندینگ:** hero (عنوان، زیرعنوان، دکمهٔ «شروع شیرجه» و «ورود»)، معرفی ۳ قابلیت، آخرین پست‌ها، فوتر. بدون شلوغی. عنوان و زیرعنوان از front matter فایل `docs/public/index.md` می‌آید؛ بقیه می‌تواند در کد باشد.
- **وبلاگ:** کارت پست (کاور، عنوان، خلاصه، تاریخ شمسی، تگ، زمان مطالعه)، صفحه‌بندی، فیلتر تگ، پست‌های مرتبط، لینک قبلی/بعدی.
- **مستندات عمومی:** همان چیدمان بخش ۱۰.۳.
- **پروژه‌ها (`/projects`):** شبکهٔ کارت‌های ریشه‌های قابل‌دسترس (آیکون، عنوان، توضیح از `index.md`). بعد از ورود، کاربر به اینجا می‌رود (یا به `next`).
- **ورود:** کارت مرکزی؛ دکمهٔ گوگل، فیلد ایمیل، ورود کد ۶ رقمی با فوکوس خودکار و پیوستن از clipboard، پیام‌های خطای روشن، شمارندهٔ ارسال مجدد.
- **۴۰۴ و خطا:** صفحه‌های ساده و هماهنگ با برند.

### ۱۰.۶ موبایل‌فرست، دسترسی‌پذیری و ریسپانسیو

مستندات همیشه Markdown ساده است و فقط رندر ریسپانسیو می‌شود، پس نویسنده هیچ کاری برای موبایل نمی‌کند؛ کل بار بر عهدهٔ قالب است.

- **موبایل‌فرست:** طراحی از ۳۶۰px شروع می‌شود و به تبلت و دسکتاپ گسترش می‌یابد.
- **viewport:** `width=device-width, initial-scale=1, viewport-fit=cover` با رعایت safe-area (ناچ آیفون).
- **ناوبری:** هدر ثابت و کوتاه (منو، لوگو، جستجو)؛ منوی جانبی به‌صورت drawer از سمت راست؛ TOC به‌صورت آکاردئون بالای صفحه.
- **جستجو:** روی موبایل تمام‌صفحه با فوکوس خودکار.
- **محتوا:** متن ۱۶px، حاشیهٔ ۱۶px؛ جدول و بلوک کد **داخل کانتینر خودشان** افقی اسکرول می‌شوند و کل صفحه هرگز اسکرول افقی ندارد؛ تصاویر `max-width: 100%`؛ آدرس‌ها و عبارت‌های لاتین طولانی با `overflow-wrap: anywhere` می‌شکنند.
- **لمس:** هدف‌های لمسی حداقل ۴۴px؛ هیچ تعاملی فقط با hover نباشد؛ دکمهٔ کپی کد همیشه دیده شود.
- **فرم ورود:** `inputmode="numeric"` و `autocomplete="one-time-code"` برای کد ۶ رقمی.
- **عملکرد:** تصاویر lazy، فونت subset با `font-display: swap`، حداقل JS روی صفحات محتوا.
- **دسترسی‌پذیری:** کنتراست AA، ناوبری کامل با کیبورد، focus قابل‌دیدن، `aria-label` فارسی، احترام به `prefers-reduced-motion`.
- **تست:** عرض‌های ۳۶۰، ۳۹۰، ۷۶۸ و ۱۲۸۰؛ Safari iOS و Chrome Android؛ در Playwright با emulation.

---

## ۱۱. سئو و آمادگی برای مدل‌های هوش مصنوعی (فقط بخش عمومی)

### سئو

- همهٔ صفحات عمومی **در زمان بیلد** به HTML استاتیک تبدیل می‌شوند (SSG، بدون ISR)
- برای هر صفحه: `title` با قالب `%s | dotdive`، `description`، `canonical`، Open Graph، Twitter Card
- **JSON-LD:** `WebSite` (با `SearchAction`)، `BlogPosting` برای پست‌ها، `TechArticle` برای مستندات، `BreadcrumbList`
- `sitemap.xml` فقط شامل صفحات عمومی با `lastmod` واقعی
- `robots.txt`: اجازهٔ بخش عمومی، `Disallow` برای `/p/`، `/api/`، `/login`، `/projects`، و اشارهٔ sitemap. توجه: `robots.txt` **امنیت** نیست؛ محافظت واقعی احراز هویت و هدر `noindex` است.
- URLهای تمیز، بدون اسلش پایانی، redirect دائمی (301) برای `aliases`
- RSS برای وبلاگ (`/rss.xml`) با `<link rel="alternate">` در head
- تصویر OG: تولید خودکار برای صفحات عمومی، در زمان بیلد و به‌صورت فایل استاتیک. **نکته:** شکل‌دهی متن فارسی در ابزارهای تولید تصویر (مثل satori) باید حتماً تست شود. اگر خروجی درست نبود، تصویر را با مرورگر headless در زمان بیلد بسازید یا از تصویر ثابت برند استفاده کنید.
- نقشهٔ محتوا: breadcrumb، پست‌های مرتبط، لینک‌دهی داخلی
- اعتبارسنجی مالکیت Search Console/Bing با متغیر محیطی
- اهداف عملکرد: Lighthouse موبایل ≥ ۹۵ در صفحات عمومی، LCP کمتر از ۲٫۵ ثانیه، CLS کمتر از ۰٫۰۵، حداقل JS روی صفحات محتوا

### آمادگی برای LLMها

- **`/llms.txt`** طبق قالب عمومی llms.txt: عنوان `#`، خلاصهٔ کوتاه در blockquote، بخش‌های `##` با فهرست لینک‌ها `- [عنوان](آدرس.md): توضیح`
- **`/llms-full.txt`:** متن کامل محتوای عمومی در یک فایل
- **نسخهٔ `.md` هر صفحهٔ عمومی** (مثلاً `/docs/x.md`، `/blog/x.md`)؛ اختیاری: `Accept: text/markdown` روی همان آدرس
- **هیچ‌چیز خصوصی** در این فایل‌ها، sitemap، RSS یا JSON-LD ظاهر نمی‌شود.

---

## ۱۲. امنیت

- ریپو Private؛ هیچ محتوای خصوصی در `web/public/`، خروجی استاتیک، یا bundle کلاینت نباشد. پیوست‌های خصوصی فقط از `/api/asset/...` با بررسی مجوز سرو شوند.
- پوشهٔ `out/private/` هرگز داخل مسیر فایل‌های استاتیک، CDN یا هر پوشهٔ قابل سرو مستقیم قرار نگیرد؛ فقط کد سرور آن را بعد از بررسی نشست می‌خواند.
- همهٔ مسیرهای خصوصی **dynamic** باشند؛ هرگز روی CDN کش نشوند.
- هدرهای امنیتی: CSP (با nonce)، HSTS، `X-Content-Type-Options`، `Referrer-Policy`، `Permissions-Policy`، محافظت clickjacking.
- Markdown با allowlist سخت‌گیرانه sanitize شود (`rehype-sanitize`)؛ HTML خام محدود.
- سکرت‌ها فقط در env؛ در لاگ‌ها ایمیل کامل و کدها ثبت نشوند.
- rate limit روی ورود و جستجو.
- به‌روزرسانی خودکار وابستگی‌ها (Dependabot) و `pnpm audit` در CI.
- ورود با گوگل: `redirect_uri` ثابت و دقیق.

---

## ۱۳. کیفیت و آزمون

### آزمون‌های واحد (Vitest)
- پردازش متن فارسی و tokenizer جستجو
- تحلیل `.env` و محاسبهٔ دسترسی هر ایمیل
- ترتیب‌دهی، slug، اعتبارسنجی front matter
- اعتبارسنجی `next` (فقط مسیر نسبی)

### آزمون‌های سرتاسری (Playwright) — **آزمون‌های نشتی الزامی هستند**

برای کاربر ناشناس ثابت شود که هیچ‌چیز خصوصی در این‌ها نیست: HTML خام، sitemap، `llms.txt`، RSS، نتایج جستجو، پاسخ‌های API، `out/public/`، و کد منبع bundle کلاینت.
- کاربر مجاز ریشهٔ A نمی‌تواند ریشهٔ B را ببیند (پاسخ ۴۰۴ یکسان)
- جریان کامل ورود با گوگل (mock) و کد ایمیل؛ کد اشتباه، منقضی، مصرف‌شده، و قفل شدن بعد از ۵ تلاش

### CI
lint، typecheck، `content:check`، آزمون‌ها، بیلد، اجرای Lighthouse روی چند صفحهٔ عمومی.

---

## ۱۴. ساختار پیشنهادی `web/`

```
web/
├─ src/
│  ├─ app/
│  │  ├─ (public)/         # /، /blog، /docs
│  │  ├─ (private)/        # /projects، /p/[root]/...
│  │  ├─ login/
│  │  └─ api/              # auth، search، asset
│  ├─ server/
│  │  ├─ content/          # Content Core (بخش ۸) — مستقل از UI
│  │  ├─ access/           # AccessContext، قواعد env
│  │  ├─ search/           # ایندکس + پردازش فارسی
│  │  └─ auth/             # ورود، OTP، نشست
│  ├─ components/          # UI
│  └─ styles/              # توکن‌ها، فونت‌ها
├─ scripts/
│  ├─ build-content.ts     # pipeline بیلد
│  └─ content-check.ts
├─ out/                    # خروجی بیلد (git-ignored)
│  ├─ public/              # استاتیک عمومی (قابل انتشار روی CDN)
│  └─ private/             # رندرشدهٔ خصوصی (فقط سرور)
├─ tests/
└─ .env.example
```

---

## ۱۵. متغیرهای محیطی

```bash
SITE_URL=https://...              # آدرس نهایی
AUTH_SECRET=                      # سکرت امضای نشست و HMAC
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
EMAIL_FROM="dotdive <login@...>"
EMAIL_PROVIDER_API_KEY=           # یا تنظیمات SMTP
STORE_URL=                        # Redis یا مسیر SQLite (OTP و rate limit)
ACCESS_ALL=
ACCESS_NONS=
ACCESS_LEMMO=
GOOGLE_SITE_VERIFICATION=         # اختیاری
```

فایل `.env.example` بدون مقدار واقعی در ریپو باشد و بیلد در نبود متغیرهای الزامی خطای واضح بدهد.

---

## ۱۶. نقشهٔ راه (Milestone)

تخمین‌ها تقریبی و برای یک توسعه‌دهنده هستند.

| # | مرحله | خروجی | Definition of Done | تخمین |
|---|---|---|---|---|
| M0 | راه‌اندازی | ساختار `web/`، CI، لینت، `.env.example`، فونت‌ها و توکن‌های طراحی | `pnpm dev/build/test` بدون خطا؛ CI سبز | ۱ روز |
| M1 | موتور محتوا | اسکن، front matter، manifest، رندر Markdown، خروجی دومسیره (`out/public` و `out/private`)، `content:check` | نمونه‌محتوا در `docs/` درست بیلد می‌شود؛ خطاها واضح‌اند | ۳ روز |
| M2 | UI و بخش عمومی | لندینگ، وبلاگ، مستندات عمومی، منو، TOC، حالت تاریک، RTL کامل | صفحات عمومی مطابق طراحی؛ موبایل‌فرست، ریسپانسیو و دسترس‌پذیر | ۵ روز |
| M3 | احراز هویت و بخش خصوصی | گوگل + کد ایمیل، `/projects`، `/p/**`، لایهٔ دسترسی، رفتار یکسان | آزمون‌های نشتی و ورود سبز | ۳–۴ روز |
| M4 | جستجو | ایندکس per-root، پردازش فارسی، command palette | جستجوی فارسی با نیم‌فاصله/ی-ک/ارقام درست؛ فیلتر دسترسی ثابت‌شده با آزمون | ۳ روز |
| M5 | سئو و LLM | sitemap، robots، RSS، JSON-LD، OG، `llms.txt`، `.md` خام | اعتبارسنجی ابزارهای سئو؛ آزمون نبودن مطالب خصوصی | ۲ روز |
| M6 | تثبیت و انتشار | عملکرد، هدرهای امنیتی، deploy، مستند README | Lighthouse ≥ ۹۵ (عمومی)، چک‌لیست بخش ۱۷ کامل | ۳ روز |

مجموع: حدود ۴ هفتهٔ کاری برای یک نفر (تقریبی).

---

## ۱۷. معیارهای پذیرش (چک‌لیست تحویل)

**محتوا**
- [ ] افزودن یک فایل `.md` جدید در `docs/` پس از deploy در منو، صفحه و جستجو ظاهر می‌شود
- [ ] `index.md` هر پوشه عنوان، آیکون، توضیح و ترتیب منو را تعیین می‌کند
- [ ] heading‌های `##/###` در TOC، منو و جستجو ظاهر می‌شوند
- [ ] front matter نامعتبر، لینک شکسته و لینک عمومی→خصوصی، بیلد را می‌شکنند

**دسترسی**
- [ ] ناشناس هیچ صفحه‌ای از `/p/**` را نمی‌بیند و ریدایرکت یکسان می‌گیرد
- [ ] ایمیل‌های مجاز فقط به ریشه‌های خودشان دسترسی دارند؛ بقیه ۴۰۴ یکسان
- [ ] اضافه/حذف ایمیل در `.env` بعد از deploy اثر می‌کند
- [ ] ورود با ایمیل غیرمجاز پاسخی نمی‌دهد که وجود یا نبودن ایمیل را لو بدهد
- [ ] هیچ محتوای خصوصی در HTML عمومی، sitemap، RSS، `llms.txt`، جستجوی ناشناس، خروجی `out/public/` یا bundle کلاینت نیست
- [ ] پوشهٔ `out/public/` به‌تنهایی روی هر هاست استاتیک سرو می‌شود و هیچ فایل یا ایندکس خصوصی ندارد

**رابط**
- [ ] همهٔ متن‌ها فارسی؛ RTL بدون نقص؛ ارقام و تاریخ‌ها شمسی
- [ ] حالت روشن/تاریک؛ موبایل، تبلت، دسکتاپ؛ ناوبری کامل با کیبورد
- [ ] در عرض‌های ۳۶۰، ۳۹۰ و ۷۶۸ پیکسل: بدون اسکرول افقی صفحه، منوی drawer، جدول و کد با اسکرول داخلی، هدف‌های لمسی حداقل ۴۴px
- [ ] `Ctrl/Cmd+K` جستجوی فارسی (با نیم‌فاصله، ی/ک عربی، ارقام) درست کار می‌کند

**سئو / LLM**
- [ ] `sitemap.xml`، `robots.txt`، `rss.xml`، `llms.txt`، `llms-full.txt` و نسخهٔ `.md` صفحات عمومی موجود و معتبر
- [ ] JSON-LD و OG برای پست‌ها و مستندات عمومی
- [ ] Lighthouse موبایل ≥ ۹۵ برای لندینگ، یک پست و یک صفحهٔ مستندات

---

## ۱۸. آمادگی برای MCP و آینده

- **Content Core (بخش ۸)** طوری نوشته شود که بدون تغییر در MCP قابل استفاده باشد. هر تابع، `AccessContext` می‌گیرد. برای MCP بعداً فقط یک Principal از نوع «توکن» اضافه می‌شود که ریشه‌های مجازش از تنظیمات می‌آید.
- نگاشت پیشنهادی ابزارهای MCP به توابع هسته: `list_projects ← listRoots`، `get_tree ← getTree`، `search ← search`، `read_doc ← getPage`.
- MCP در همین مونوریپو (پوشهٔ جدا مثلاً `mcp/`) یا بعداً با تصمیم جدید ساخته می‌شود. در نسخهٔ ۱ هیچ کدی برای MCP لازم نیست، ولی مرز ماژول‌ها باید رعایت شود.
- توسعه‌های محتمل بعدی: API نوشتن با توکن (commit به گیت‌هاب)، نقش‌ها، لینک‌های برگشتی (backlinks)، جستجوی معنایی، آمار بازدید حریم‌خصوصی‌محور.

---

## ۱۹. سؤال‌های باز و پیش‌فرض‌ها

| # | سؤال | پیش‌فرض |
|---|---|---|
| ۱ | ریشه‌ای که «lemmo» نوشته شده دقیقاً همین نام است یا با پروژهٔ Lemu یکی است؟ | همان `lemmo` که در سند آمده |
| ۲ | میزبانی نهایی؟ | شروع روی Vercel رایگان یا VPS با Docker؛ بعداً قابل تغییر |
| ۳ | سرویس ایمیل و دامنهٔ فرستنده؟ | Resend یا SMTP؛ دامنهٔ خود پروژه با SPF/DKIM/DMARC |
| ۴ | دامنهٔ نهایی سایت؟ | تعیین‌شده بعداً؛ همه‌جا از `SITE_URL` استفاده شود |
| ۵ | فایل‌های غیر Markdown در ریشه‌ها؟ | فقط `.md` و پیوست‌ها؛ MDX نه (تصمیم قطعی)؛ انواع دیگر فقط در صورت نیاز |
| ۶ | آدرس بخش خصوصی؟ | پیشوند `/p/<root>/...` |
| ۷ | تاریخ شمسی یا میلادی؟ | شمسی در رابط |
| ۸ | ثبت آمار بازدید؟ | فعلاً نه؛ در آینده ابزار حریم‌خصوصی‌محور |
| ۹ | دسترسی چند‌سطحی (مثلاً ویرایش)؟ | فقط خواندن |
| ۱۰ | نام‌گذاری `public/blog` و `public/docs` برای بخش عمومی؟ | همین دو پوشه؛ بقیهٔ محتوای `public` در صورت نیاز بعداً نگاشت می‌شود |

---

## ۲۰. مراحل شروع برای توسعه‌دهنده

1. ریپو را clone کن و ساختار `docs/` را با یک `index.md` برای هر ریشه و چند فایل نمونه بساز.
2. M0 را کامل کن و در PR اول توکن‌های طراحی و فونت‌ها را نشان بده تا ظاهر تأیید شود.
3. قبل از شروع M3، انتخاب کتابخانهٔ احراز هویت، سرویس ایمیل و ذخیرهٔ موقت را با مستندات فعلی‌شان تأیید و نتیجه را در README ثبت کن.
4. آزمون‌های نشتی (بخش ۱۳) را **همراه** M3 بنویس، نه بعد از آن.
5. هر milestone با PR جدا و مطابق DoD جدول بخش ۱۶ تحویل شود.