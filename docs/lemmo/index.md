---
title: "پروژه لیمو (Lemmo)"
description: "استودیو خلاقیت هوش مصنوعی — مستندات مرجع، معماری و منبع یگانه حقیقت (SSOT)"
order: 0
icon: "sparkles"
---

| فیلد / Field | مقدار / Value |
| :--- | :--- |
| **Title (EN)** | Lemmo Documentation Guidelines & Single Source of Truth (SSOT) |
| **Title (FA)** | راهنمای جامع مدیریت مستندات، پوشه‌بندی و قوانین پروژه Lemmo |
| **ID** | DOC-META-001 |
| **Category** | `architecture` |
| **Status** | `Active` |
| **Owner** | Core Engineering Team |
| **Last Updated** | 2026-09-25 |
| **Summary (EN)** | Central index, folder governance, authoring standards, and architectural blueprints for Lemmo. |
| **Summary (FA)** | راهنمای مرکزی ساختار پوشه‌ها، قوانین مستندنویسی، جدول متادیتا و معماری پروژه لیمو. |
| **Tags** | `docs`, `governance`, `ssot`, `standards`, `lemmo` |

---

# مستندات مرجع پروژه Lemmo — منبع یگانه حقیقت (SSOT)

> **اصل طلایی (The Golden Rule):**  
> در کل اکوسیستم نرم‌افزاری Lemmo برای هر قانون معماری، مقدار دیزاین‌توکن و تصمیم فنی، **دقیقاً یک منبع موثق (Single Source of Truth)** وجود دارد. ثبت اطلاعات تکراری یا متناقض در چند سند ممنوع است. این بخش مرجع رسمی، الزام‌آور و معتبر برای تمامی اعضای تیم و ایجنت‌های هوش مصنوعی می‌باشد.
> 
> 🤖 **الزامات ایجنت‌های هوش مصنوعی:** کلیه مدل‌ها و ایجنت‌های توسعه پیش از ایجاد یا ویرایش هر سند، موظف به رعایت پروتکل توقف و قوانین ضدتضاد در سند [**`09-governance/rules.md`**](./09-governance/rules.md) و [**`01-architecture/AGENTS.md`**](./01-architecture/AGENTS.md) هستند.

---

## 🗂️ نقشه‌راه و راهنمای جهت‌یابی بخش‌های لیمو

| حوزه تخصصی | مسیر بخش | محتوا و وظیفه |
| :--- | :--- | :--- |
| 🏛️ **معماری و تصمیمات کلان** | [`01-architecture/`](./01-architecture/) | قوانین مرزهای کد، پروتکل ایجنت‌ها (`AGENTS.md`) و سوابق رسمی تصمیمات معماری (`decisions/`) |
| 💻 **توسعه فرانت‌اند** | [`02-frontend/`](./02-frontend/) | معماری مرجع Workspace در Next.js، تصمیمات سیستم پلاگین و وابستگی‌ها |
| ⚙️ **توسعه بک‌اند و سرویس‌ها** | [`03-backend/`](./03-backend/) | معماری میکروسرویس‌های lemmo-api، کاتالوگ سرویس‌ها، قراردادهای Proto و راهنمای استایل Go |
| 🎨 **دیزاین سیستم و کامپوننت‌ها** | [`04-design-system/`](./04-design-system/) | مشخصات پالت رنگ، تایپوگرافی، فواصل، پریمیتیوها، آیکون‌ها، چیدمان و دسترس‌پذیری |
| 🏷️ **برندینگ و هویت بصری** | [`05-branding/`](./05-branding/) | اصول برند Lemmo، نشان ترایاد ۳ نقطه‌ای، استانداردهای اندازه و عدم اعوجاج لوگو |
| 🧩 **ماژول‌ها و فیچرها** | [`06-modules/`](./06-modules/) | مشخصات عملکردی ماژول‌های مستقل (موتور ابزار، چت، کانواس، دارایی‌ها و پرداخت) |
| 🚀 **زیرساخت و دوآپس** | [`07-devops/`](./07-devops/) | خطوط تست و استقرار مداوم (CI/CD)، فایل‌های داکر، استراتژی‌های کش و CDN و امنیت سرورها |
| 🗺️ **نقشه راه و فازبندی‌ها** | [`08-roadmap/`](./08-roadmap/) | برنامه زمان‌بندی فازها، تاییدیه‌ها، چک‌لیست‌های QA و گزارش‌های تحویل مراحل پروژه |
| 📋 **قوانین و استانداردهای توسعه** | [`09-governance/`](./09-governance/) | قوانین ضدتضاد SSOT و الگوهای آماده جهت ایجاد سند جدید با جدول متادیتا |

---

## 📌 قانون اجباری: جدول متادیتای بالای هر سند (Document Metadata Header)

برای خوانش سریع توسط انسان، جستجوی راحت موضوعی و **پارس خودکار توسط اسکریپت‌های هوش مصنوعی (LLMs)**، **تمام فایل‌های مستندات جدید باید در بالاترین سطر خود دارای یک جدول متادیتا باشند**.

### ساختار جدول متادیتا:
```markdown
| فیلد / Field | مقدار / Value |
| :--- | :--- |
| **Title (EN)** | [عنوان استاندارد انگلیسی سند] |
| **Title (FA)** | [عنوان رسمی فارسی سند] |
| **ID** | DOC-[CATEGORY]-[NUM] (مانند DOC-FE-001 یا DOC-ARCH-002) |
| **Category** | `architecture` \| `design-system` \| `branding` \| `frontend` \| `backend` \| `devops` \| `modules` \| `roadmap` |
| **Status** | `Draft` (پیش‌نویس) \| `Active` (فعال) \| `Approved` (تأییدشده) \| `Deprecated` (منسوخ) |
| **Owner** | [تیم یا شخص مسئول - مثلاً Frontend Team / @username] |
| **Last Updated** | YYYY-MM-DD (تاریخ میلادی آخرین تغییر) |
| **Summary (EN)** | [یک یا دو جمله خلاصه انگلیسی جهت استفاده در ایندکس و هوش مصنوعی] |
| **Summary (FA)** | [یک یا دو جمله خلاصه فارسی جهت درک سریع مخاطب] |
| **Tags** | `برچسب۱`, `برچسب۲`, `برچسب۳` |
```

> 💡 **نکته مهم:** حتماً از الگوی آماده در مسیر [`09-governance/document-template.md`](./09-governance/document-template.md) کپی کنید.

---

## ✍️ راهنمای گام‌به‌گام نگارش مستندات جدید (Authoring Standards)

توسعه‌دهندگان هنگام افزودن هرگونه مستند باید گام‌های زیر را رعایت نمایند:

### گام اول: انتخاب پوشه مناسب
- سند مربوط به چه حوزه‌ای است؟ مستندات را فقط در یکی از پوشه‌های استاندارد جدول فوق قرار دهید. از ساخت پوشه‌های بی‌نام یا موازی خودداری فرمایید.

### گام دوم: استفاده از الگو
1. فایل [`09-governance/document-template.md`](./09-governance/document-template.md) را در پوشه مقصد با نامی با فرمت کباب‌کیس (`kebab-case.md`) کپی کنید.
2. جدول متادیتا را با دقت پر کنید. فیلدهای **Title (EN)** و **Summary (EN)** به زبان انگلیسی بسیار مهم هستند زیرا مستقیماً در تولید ایندکس مدل‌های زبانی استفاده می‌شوند.

### گام سوم: نگارش متن و ساختار
- **زبان مستندات:** متن بدنه مستندات به زبان **فارسی سلیس و حرفه‌ای فنی** نگارش می‌شود؛ اما اصطلاحات تخصصی کد و متادیتاهای ماشینی در کنار معادل فارسی درج می‌گردد.
- **عناوین و سرفصل‌ها:** از `#` فقط یک‌بار برای عنوان اصلی سند استفاده کنید. بخش‌ها با `##` و زیربخش‌ها با `###` تفکیک شوند.
- **بلوک‌های کد:** همیشه نوع زبان برنامه‌نویسی را بعد از سه بک‌تیک مشخص نمایید (` ```typescript `، ` ```bash `، ` ```json `).
- **دیاگرام‌ها:** برای نمایش جریان کار، از دیاگرام‌های متنی استاندارد **Mermaid** (` ```mermaid `) استفاده کنید.
- **لینک‌های معتبر:** به جای آدرس‌های مطلق محلی سیستم خود، از مسیرهای نسبی مارک‌داون (مانند `[متن](./02-frontend/index.md)`) استفاده نمایید.

---

## 🤖 اسکریپت تجمیع مستندات برای هوش مصنوعی (`llms.txt`)

برای آنکه مدل‌های هوش مصنوعی (LLMs) بتوانند به صورت خودکار و ساختاریافته به کلیه مستندات، توکن‌ها و معماری پروژه دسترسی داشته باشند، این مخزن از استاندارد جهانی [llmstxt.org](https://llmstxt.org/) پشتیبانی می‌کند.

### نحوه عملکرد اسکریپت:
اسکریپت [`scripts/generate-llms.py`](./scripts/generate-llms.py) به صورت خودکار:
1. تمام فایل‌های مارک‌داون موجود در مستندات را پیمایش می‌کند.
2. جدول متادیتای هر سند را می‌خواند و مقادیر **Title (EN)**، **Summary (EN)** و **Category** را استخراج می‌کند.
3. فایل ساختاریافته و انگلیسی [`llms.txt`](./llms.txt) را بر اساس دسته‌بندی‌های استاندارد در ریشه مستندات تولید و به‌روزرسانی می‌نماید.

### نحوه اجرای اسکریپت:
پس از ایجاد یا ویرایش هر سند، دستور زیر را در ترمینال اجرا کنید:
```bash
python3 scripts/generate-llms.py
```
خروجی در فایل `llms.txt` بازتولید شده و آماده بهره‌برداری توسط تمامی ایجنت‌های توسعه و مدل‌های پردازش زبان خواهد بود.

---

## ⚖️ قوانین تغییر معماری (ADR Governance)
- برای هرگونه تغییر در استک، تصمیم فنی کلان یا تغییر در مدل داده، باید یک **رکورد تصمیم معماری (ADR)** در پوشه [`01-architecture/decisions/`](./01-architecture/decisions/) مطابق الگوی [`01-architecture/decisions/_template.md`](./01-architecture/decisions/_template.md) ثبت شود.
- تغییر کدها بدون ثبت مستندات تاییدشده معماری در این پوشه، در فرآیند Code Review رد خواهد شد.

## معماری و استانداردهای بک‌اند (`lemmo-api`)

مستندات بک‌اند بر اساس معماری مونوریپوی nons-api و استانداردهای رسمی تدوین شده‌اند: از [نمایه بک‌اند](./03-backend/index.md) شروع کنید. [معماری میکروسرویس‌ها](./03-backend/architecture.md)، [شناسنامه سرویس‌ها و فازبندی](./03-backend/services.md)، [قراردادها و پاکت رویدادها](./03-backend/contracts.md) و [راهنمای استایل کدنویسی Go](./03-backend/style-guide.md) اسناد رسمی مصوب برای توسعه مخزن `api` هستند.


